// Split schema implementation lives in ./schema/*.
// Keep this file as a stable entrypoint for:
// - drizzle-kit (schema path)
// - runtime schema imports (Drizzle `schema` option)

export * from './schema/index';
